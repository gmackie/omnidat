#!/usr/bin/env sh
set -eu

runtime_dir="${OMNIDAT_E2E_DIR:-build/e2e-omnibank}"
rm -rf "$runtime_dir"
mkdir -p "$runtime_dir"

python3 -m tools.omnidat_omnibank e2e-card-sale --runtime-dir "$runtime_dir"
